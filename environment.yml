name: sella
channels:
  - defaults
  - conda-forge
dependencies:
  - python >= 3.9
  - pip
  - numpy >= 1.14.0
  - scipy >= 1.1.0
  - cython >= 0.29.23
  - ase >= 3.22.0
  - pytorch >= 2.0
  - setuptools >= 74.1.0, < 82
  - setuptools-scm >= 8.0
